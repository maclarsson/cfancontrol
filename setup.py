from setuptools import setup

# command for system wide install:
# sudo python setup.py install --single-version-externally-managed --root=/
# -or-
# sudo pip install -U .

if __name__ == "__main__":
    setup()
